(ns cim_portfolio.regression)

(defn get-alpha-beta
  "Stubbed get-alpha-beta for environments without libpython.
   Returns empty plotting data so the rest of the site can compile."
  [stock-data market-data]
  {:plotted-dates []
   :plotted-alpha []
   :plotted-beta []})
